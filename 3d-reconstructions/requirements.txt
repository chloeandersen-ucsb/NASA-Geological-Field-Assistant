# Core helper libs (not including TripoSR itself).
#
# We intentionally do NOT list TripoSR here because Jetson aarch64 may require
# source install or platform-specific wheels. Install TripoSR separately:
#   pip install triposr
# or clone and `pip install -e .`.
#
# Pinned optional utilities for mesh conversion and inspection.

# Base dependencies
numpy>=1.24,<2.0
trimesh>=4.0,<5.0
Pillow>=10.0

# ZoeDepth dependencies (for scene reconstruction)
timm>=0.9.0
opencv-python-headless>=4.8.0

# Video processing (for video-to-3D pipeline)
opencv-python>=4.8.0  # Video frame extraction
scikit-image>=0.21.0  # SSIM similarity metrics

# Configuration management
PyYAML>=6.0

# Point cloud processing
# Note: Use open3d-cpu on systems without GPU rendering support
# For Jetson, install open3d from source or use prebuilt wheel
# open3d>=0.17.0




